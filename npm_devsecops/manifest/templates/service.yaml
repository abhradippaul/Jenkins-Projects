apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejs-devsecops.fullname" . }}
  labels:
    {{- include "nodejs-devsecops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "nodejs-devsecops.selectorLabels" . | nindent 4 }}
